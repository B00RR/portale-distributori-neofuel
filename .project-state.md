# Stato Progetto — Neofuel Portal Lint Cleanup

## Repo & Branch
- Path: `/home/b00/neofuel-work`
- Remote: `origin`
- Branch corrente: `fix/issues-3-8-12`
- Target: `npm run lint` → 0 errori, 0 warning (`--max-warnings 0`)

## Baseline e vincoli
- `npx tsc --noEmit` deve rimanere pulito.
- `npm test` deve rimanere 461 passed / 1 skipped.
- Non toccare `tests/` né `e2e/` (sono fuori dal pattern lint ma i test devono passare).
- Non includere mai credenziali nei commit/log.

## Progresso
- Commits effettuati:
  1. `fix(lint): admin core — safe DOM, types, logger, no explicit-any (#3 #8 #12)`
  2. `fix(lint): admin CRUD — guns, invoices, islands, prices, stations, tanks (#3 #8 #12)`
  3. `fix(lint): UI components — toast, AlertBox, BaseComponent, DataTable, FormField, ShiftOpener (#3 #8 #12)`

## Conteggio problemi lint
- Iniziale: 65 file, 88 errori, 339 warning → 428 problemi.
- Attuale: 413 problemi (57 errori, 356 warning) su molti file.

## File con problemi rimanenti (principali)
- `js/admin/analytics.ts`
- `js/admin/dashboard-charts.ts`
- `js/admin/dashboard-config.ts`
- `js/admin/dashboard.ts`
- `js/admin/operators.ts`
- `js/admin/vouchers_reboot.ts`
- `js/admin/router.ts`
- `js/admin/shifts.ts`
- `js/admin/ui-settings-panel.ts`
- `js/ui/components/ClosureWizard.ts`
- `js/ui/components/VoucherManager.ts`
- `js/core/*` (molti file)
- `js/operator/*` (molti file)
- `js/utils/sanitizer.ts`
- `js/utils/utils.ts`
- `js/vite-env.d.ts`

## Strategia
- Spezzettare il lavoro in lotti di 1-3 file correlati.
- Delegare a subagent i file più pesanti (operatori, voucher_reboot, dashboard-config, ClosureWizard).
- Fixare manualmente i file piccoli/medi mentre i subagent lavorano in background.
- Ogni lotto verificato con `npx eslint <file>` (0 errori, 0 warning), `npx tsc --noEmit` pulito, `npm test` verde.
- Commit atomico per ogni lotto completato.
- Push finale del branch solo quando `npm run lint` passa al 100%.
