# Stato Progetto — Neofuel Portal Lint Cleanup

## Repo & Branch
- Path: `/home/b00/neofuel-work`
- Remote: `https://github.com/B00RR/portale-distributori-neofuel` (privato)
- Branch attivo: `fix/issues-3-8-12`

## Goal
Portare `npm run lint` a 0 errori e 0 warning (`--max-warnings 0`), completando le issue #3 (TS), #8 (eslint) e #12 (`as any`/`@ts-ignore`).

## Vincoli
- Ogni batch deve lasciare `npx tsc --noEmit` pulito.
- `npm test` deve rimanere 461 passed / 1 skipped.
- NON modificare `tests/` né `e2e/` per la lint: i test devono restare intatti.
- Commit atomici, uno per area.
- Non pushare su `main`; pushare solo sul branch di lavoro.

## Progresso

### Lotto 1 — Admin core (completato, da committare)
File puliti:
- `js/admin.ts`
- `js/admin/components/FilterBar.ts`
- `js/admin/components/Pagination.ts`
- `js/admin/credits.ts`
- `js/admin/layout.ts`
- `js/admin/notifications.ts`
- `js/ui/ui.ts`
- `js/shared/state.ts` (modifica di supporto)

Verifiche passate:
- `npx eslint` sui file del lotto: 0 errori, 0 warning
- `npx tsc --noEmit`: pulito
- `npm test`: 461 passed, 1 skipped

### Lotti successivi (pending)
- l2a: guns, invoices, islands, prices, stations, tanks
- l2b: operators, vouchers_reboot
- l3: dashboard, dashboard-charts, dashboard-config, router, shifts, ui-settings-panel
- l4: UI components (AlertBox, BaseComponent, ClosureWizard, DataTable, FormField, ShiftOpener, VoucherManager, toast)
- l5: core + app.ts
- l6: operator modules
- l7: utils/shared/types + vite-env.d.ts

## Note
- I test sono stati accidentalmente modificati da un subagent precedente; sono stati ripristinati con `git checkout -- tests/ e2e/`.
- Molti file hanno `/* eslint-disable @typescript-eslint/no-explicit-any */` in testa: vanno rimossi e sostituiti con tipi corretti o singoli `eslint-disable-next-line` giustificati.
- I warning `security/detect-object-injection` sono risolvibili usando `Map` o `Object.prototype.hasOwnProperty.call`.
- I warning `no-console` vanno convertiti in `logger.info/debug/error` o rimossi.
- I warning `no-unsanitized/property` (innerHTML) richiedono costruzione DOM sicura.
